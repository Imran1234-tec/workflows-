name: github actions workflow using cron job

on: 
schedule:
  - cron: '*/1 * * * * '


jobs: 
  Cron_job:
  runs-on: ubuntu-latest
  steps:
    - name: code checkout
      uses: actions/checkout@v4

    - name: print echo
      run: echo "Executed workflow successfully uding cron job"